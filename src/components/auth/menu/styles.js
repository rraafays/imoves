import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    padding: 30
  },
  header_text: {
    textAlign: 'center',
    fontSize: 24,
    fontWeight: 'bold',
    color: '#FFFFFF'
  },
  text_box: {
    margin: 30,
    borderColor: '#FFFFFF',
    borderWidth: 2,
    borderStyle: 'solid',
  }
});

export default styles;
